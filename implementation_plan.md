# Booking Lifecycle & Notification System (Talabat/Uber Model)

هدفنا بناء نظام متكامل ومحكم منطقياً للتعامل مع الحجوزات بحيث تمر بدورة حياة كاملة (Lifecycle) مع إشعارات لحظية للطرفين (اللاعب وصاحب الملعب) زي ما بيحصل في تطبيقات أوبر وطلبات.

## دورة الحياة الجديدة (The Strict Lifecycle)
1. **الطلب (Pending):** اللاعب بيطلب حجز الميعاد، الميعاد بيتحجز مؤقتاً في الداتا بيز بحالة PENDING. بيتم منع أي لاعب تاني من اختياره في الوقت ده.
2. **إشعار للمالك:** صاحب الملعب بيوصله إشعار (Notification) إن فيه حجز جديد في الانتظار.
3. **قرار المالك (Accept/Reject):**
   - **القبول (Confirmed):** المالك بيضغط "تأكيد" في الداشبورد. بيتبعت إشعار للاعب إن الحجز اتأكد، والميعاد بيفضل مقفول.
   - **الرفض (Rejected):** المالك بيضغط "رفض". بيتبعت إشعار للاعب بالرفض، والميعاد بيرجع يفتح تاني للناس في الأبلكيشن.

## Proposed Changes

### Backend (src/index.ts)
- [MODIFY] POST /api/bookings: جعل حالة الحجز الافتراضية PENDING. تعديل كود الـ (Pessimistic Lock) بحيث يمنع الحجز لو الميعاد كان CONFIRMED أو PENDING. وإنشاء Notification للمالك.
- [NEW] PATCH /api/bookings/:id/status: Endpoint جديدة للمالك لتغيير حالة الحجز (CONFIRMED/REJECTED) مع توليد إشعار فوري للاعب.
- [NEW] GET /api/notifications: Endpoint لجلب إشعارات المستخدم (سواء لاعب أو مالك).
- [NEW] PATCH /api/notifications/read-all: Endpoint لتحديد كل الإشعارات كمقروءة.

### Mobile App (Flutter)
- [MODIFY] lib/screens/pitch_details_screen.dart: قراءة الحجوزات سواء CONFIRMED أو PENDING باللون الأحمر لضمان عدم حدوث تعارض، ورسالة الحجز للمستخدم تتغير لـ "تم إرسال طلب الحجز لانتظار موافقة المالك".
- [MODIFY] lib/screens/owner_dashboard_screen.dart: قسم الحجوزات هيتقسم لـ "حجوزات جديدة في الانتظار" (بأزرار قبول ورفض) و"حجوزات مؤكدة".
- [NEW] lib/screens/notifications_screen.dart: شاشة جديدة تعرض الإشعارات للمالك واللاعب.
- [MODIFY] lib/screens/home_screen.dart: إضافة أيقونة جرس (Bell Icon) للإشعارات فيها عداد (Badge) بالإشعارات غير المقروءة.

## Verification Plan
1. الدخول بحساب لاعب لطلب حجز (PENDING).
2. التأكد من منع لاعب آخر من حجز نفس الميعاد.
3. الدخول بحساب المالك (صاحب الملعب) وتأكيد ظهور الإشعار.
4. قيام المالك برفض الحجز، والتأكد من وصول الإشعار للاعب، وفتح الميعاد مجدداً في الشاشة.
5. إعادة الحجز من اللاعب وقبوله من المالك، والتأكد من إتمام العملية بالكامل.
